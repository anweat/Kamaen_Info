# 验证记录

## 2026-09-10 · 开发工具入口

新增 `#/development`：连接当前 Java 工程、Blockbench、NBT Studio、模型源目录及游戏资源目录。官方 MDK 为独立参考克隆，未替换现有 Mod 工程。

- Wiki 生产构建通过；自动测试共 13 项通过，新增桌面桥的启动目标、请求来源、未知入口、任意参数和缺失文件校验。
- Chrome 检查 8 个入口状态，320/768/1440 像素无横向溢出；网页工具按钮实际启动桌面应用。NBT Studio 的 .NET 兼容启动和测试样本加载已确认。
- 当前 Mod 工程在 Java 21 下构建成功，并通过 `tooling/dev.ps1 build` 再次验证。未执行游戏内验收。
- 版本、安装来源、本机兼容策略及剩余 Blockbench MCP 边界见 [开发工具链](../tooling/README.md)。截图位于忽略目录 `output/development-desktop.png` 和 `output/development-nbt.png`。

## 2026-09-09 · 时空观测台视觉更新

- `npm run build`、TypeScript 检查及 `npm test` 全部通过，12 项自动测试未回退。现有离线文档 content 分包仍有体积提示（约 1.41 MB，gzip 545 KB）。
- 独立 Chrome 会话完成 27 项主题交互检查和 17 项地图/实验页面回归检查，无运行异常或回归脚本捕获的控制台错误。
- 观测索引按实体去重，关联跳转累积、重复打开不重复计数，刷新后恢复。波形幅度随信息积累变化；1.2 Hz 对应约 0.8333 秒的真实 CSS 周期。
- 暂停环境动效以及系统减少动态效果，均关闭波形和详情背景动画；暂停后仍可打开、关闭和阅读实体。详情正文实测对比度约 9.36:1（该段正文与弹窗底色），未据此宣称全站无障碍审计完成。
- 320、768、1024、1440 像素视口无页面横向溢出；窄屏频率设置可从顶栏打开并保持在视口内。桌面文档、配方、实体、模型页面均检查布局并截图。
- 地图对象阅读前后坐标与尺寸不变；拖动后的绑定关系线跟随、撤销恢复、键盘调整大小、原位详情随窗口收缩、专注模式、3D 剖层/拆解/播放、独立 HTML 滑块均回归通过。测试拖动与尺寸调整已撤销并保存恢复。

视觉源文件位于 `src/visual/`。本次时空参考面使用程序化 SVG，保留可缩放精度；现有像素贴图继续作为示例素材。独立信号窗口的示例 HTML 同步使用深色仪器配色。

本地验证产物（Git 忽略）：`output/observatory-check.js`、`output/observatory-regression.js`，以及 `observatory-refined.png`、`observatory-dialog.png`、`observatory-mobile.png`、`observatory-mobile-settings.png`、`observatory-docs.png`、`observatory-recipes.png`、`observatory-entities.png`、`observatory-models.png`。

## 2026-09-09 · 自由画布与文件注册

本轮只修改 `wiki/`。原 Mod 代码、rebuild 源文档以及旧画布内容保留。

- `npm run validate:content` 通过：8 个文件实体、2 张地图、2 个场景、1 个模型、1 个自定义渲染。
- `npm run build` 通过 TypeScript 和生产构建；当前同步 73 份 rebuild 文档。离线文档包约 1.41 MB（gzip 约 545 KB），Vite 提示分包较大，尚未按文档延迟加载。
- `npm test` 共 12 项通过。新覆盖自由坐标、缩放变换、跟随连线、删除目标后的端点保留、笔迹缩放、模型表达式、文件自动注册、重复/缺失引用，以及地图原子保存、外部文件修改与并发哈希冲突。
- 独立 Chrome 会话两组浏览器检查共 38 项通过，未捕获页面异常或控制台错误。

浏览器覆盖：

| 范围       | 验证结果                                                                                                      |
| ---------- | ------------------------------------------------------------------------------------------------------------- |
| 原位详情   | 原生模态背景不可交互、图文与资源正确显示、关联替换、Escape 关闭、焦点返回；所有地图对象坐标尺寸保持不变       |
| 自由编辑   | 拖动与点击区分、按缩放率移动、小数坐标与尺寸保留、虚线框、自由画笔、箭头、文字编辑                            |
| 关系与尺寸 | 连线随目标移动，撤销恢复；键盘可调整贴图尺寸                                                                  |
| 保存       | 文件保存后刷新恢复对象、文字和笔迹；测试用空白地图已通过导入与保存恢复为空，并通过 API 再次核对               |
| 文件注册   | 仅新增 `waveguide-chip.json` 和说明文件，条目自动出现在组件库和实体总览，独立 Markdown 档案可访问             |
| 实验页面   | NBT 字段表格；文件场景剖层、拆解与播放；2D 参数曲线；sandbox HTML 中的信号滑块                                |
| 响应与视口 | 320、768、1024、1440 像素下无页面横向溢出；已打开弹窗随窄屏重新限制边界；专注画布占满视口；滚轮缩放不滚动页面 |

本地验证产物（`output/` 被 Git 忽略）：`free-map-check.js`、`free-map-pages-check.js`、`free-map-desktop.png`、`free-map-focus.png`、`free-map-spotlight-final.png`、`free-map-mobile.png`、`free-map-mobile-dialog.png`。

验证中修正了切换地图读取期间可过早导入的问题：读取完成前禁止编辑和导入，避免服务端初始读取覆盖新草稿。浏览器脚本还明确等待导入状态，并在恢复示例后检查磁盘内容，避免只凭旧成功提示判断保存完成。

边界：移动端为 Chrome 视口模拟，未验证真实多指触控或完成屏幕阅读器审计。3D 为体素场景；未实现 glTF 或 NBT 二进制导入。2D 为声明式参数曲线；任意独立展示通过 HTML 渲染入口扩展。没有多人协作服务或游戏运行时注册生成器。

## 2026-09-07 · 旧章节团簇版本

日期：2026-09-07。范围：本地 Wiki；未修改或验证 Minecraft 游戏代码。

## 自动检查

- `npm run build`：TypeScript 检查和 Vite 生产构建通过；同步 53 份 rebuild 文档。
- `npm test`：7 项通过，覆盖实体与配方引用、画布数据校验、展开/换选/收起回流、本地笔记校验、原子保存、并发版本冲突和同源限制。
- 构建存在体积提示：包含离线文档的 content 分包约 1.05 MB（gzip 约 393 KB）。当前首版随站点载入，后续可按文档延迟加载。

## Chrome 浏览器实测

使用独立 Playwright Chrome 会话，访问 `http://127.0.0.1:4173`。

| 范围       | 已验证行为                                                                             |
| ---------- | -------------------------------------------------------------------------------------- |
| 团簇内展开 | 点击显示原位详情；组件增高；下游位移与增高一致；正文不截断；再次点击恢复原高度与位置   |
| 关联与换选 | 关联指引在目标团簇打开条目；替换详情不会累积高度偏移                                   |
| 布局操作   | 键盘移动、缩放、撤销；按当前缩放率拖动；组件收起与展开；新增和编辑文字组件             |
| 持久化     | 保存后刷新恢复布局与展开条目；移除当前展开条目后详情关闭，撤销恢复；再次收起恢复原高度 |
| 视口       | 320、768、1024、1440 像素宽度下页面无横向溢出；画布以平移和缩放访问空间内容            |
| 笔记       | Markdown 物品贴图引用、场景嵌入、保存后刷新、离开页面再返回恢复未保存草稿              |
| 贴图索引   | 默认图标模式、E1 条目数量、搜索过滤、固定快速说明、切换表格模式保留过滤                |
| 3D 示意    | 爆炸视图、层级滑块、分步播放和暂停                                                     |
| 运行错误   | 上述浏览器检查过程中未捕获页面 JavaScript 异常                                         |

可重跑的检查脚本和截图位于本地忽略目录 `output/`：`browser-check.js`、`browser-notes-check.js`、`canvas-mobile.png`、`final-canvas.png`。这些是开发验证产物，不随源代码提交。

## 验证边界

材质和结构为占位或示意；关系示例不证明游戏配方可执行。移动端检查使用桌面 Chrome 调整视口，未验证真实触摸设备，也未完成屏幕阅读器审计。文件写入验证针对本机开发服务；静态托管仅支持浏览器草稿和导出，未实现多人协作服务。
