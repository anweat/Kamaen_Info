# R3.3局部验算：热、振动源与线缆路径

日期：2026-09-07。由[r3_rack_environment_probe.py](r3_rack_environment_probe.py)生成；[机器可读结果](r3-rack-environment-results.json)。

配套[机柜环境与工程玩法](COMPUTING_R3_RACK_ENVIRONMENT_AND_ENGINEERING.md)。下列三个工况彼此独立，所有参数均由作者指定，没有拟合真实硬件。

## 热模型

封闭两节点：C分别为100、200 J/K，初始60、20°C，G=2 W/K，步长1模型秒，结果为59.2、20.4°C；内部交换不改变总储热。

单节点热阶跃：C=600 J/K，G=6 W/K，持续耗散120 W，固定外部20°C，时间常数100模型秒，稳态40°C。该例直接计算解析解；一般热网络例子采用有步长约束的显式更新。

| 工况 | 结果 |
| --- | --- |
| 开机60模型秒 | 29.0238°C |
| 开机300模型秒 | 39.0043°C |
| 此后停机60模型秒 | 30.4298°C |

空气回流是另一个独立稳态例子：固定新风20°C、气流接收120 W，T_out=T_in+P/H，T_in=(1-r)T_supply+rT_out。H和r是给定边界，未由柜体几何求解。

| 给定风路 | 入口 | 出口 |
| --- | --- | --- |
| H=12 W/K，r=0.25 | 23.3333°C | 33.3333°C |
| H=18 W/K，r=0.05 | 20.3509°C | 27.0175°C |

## 振动与相关源

单自由度基座激励，阻尼比0.1：频率比1的幅度传递率为5.099020；频率比3为0.145366。这不是任意支撑串接后的乘积模型。

同一频段的复RMS响应：同源同相0.6、0.4先相加，得到功率1.00；明确独立的两个源得到功率0.52。只有此处明确独立的源才做平方和。

共架相对位移为零的例子不证明晶振对整体加速度不敏感。未求多自由度装配、机械回路或真实冲击。

## 线缆几何

用三个点(0,0,0)→(4,0,0)→(4,4,0)，拐角圆弧半径0.5、最小允许0.4；两端各要求0.75直出长度。单位为本例局部长度单位，不等于世界方块或毫米。

原折线长8；裁去两段各0.5，加入四分之一圆弧，实际曲线长7+π/4=7.785398。校验与未来渲染应使用同一曲线定义。

本例只检查圆弧局部容纳、最小半径、直出段、端口切向和旋转不变性；没有实现曲线生成渲染、障碍碰撞、线束容量或寻路。

## 检查结果

共29项指定检查通过。

| 检查 | 结果 |
| --- | --- |
| 封闭两节点内部导热不产生或消灭热量 | 通过 |
| 热接触同时使热端降温、冷端升温 | 通过 |
| 储热变化等于输入热量减外部净输出 | 通过 |
| 无冷却时热量留在局部储热中 | 通过 |
| 步长过大被拒绝，不能伪装成物理失稳 | 通过 |
| 负导热不作为被动接触 | 通过 |
| 同一负载下短时温度与持续温度不同 | 通过 |
| 停机60模型秒后仍有余热 | 通过 |
| 增大热容延缓升温但不改变此模型的稳态 | 通过 |
| 回流入口满足新风与回流混合条件 | 通过 |
| 流体温升携带指定热量 | 通过 |
| 指定改善边界下入口出口温度均降低 | 通过 |
| 零流量不能调用有流稳态式 | 通过 |
| 完全回流不能伪装成有新风稳态 | 通过 |
| 低频极限为跟随基座 | 通过 |
| 指定隔振系统在共振处放大 | 通过 |
| 同一系统在频率比3处隔离 | 通过 |
| 增加阻尼降低该系统的共振峰 | 通过 |
| 同源同相路径先合成再求功率 | 通过 |
| 明确独立源按功率相加 | 通过 |
| 同源等幅反相路径可相消 | 通过 |
| 共架同位移两点的相对运动为零 | 通过 |
| 圆弧线路长度包含裁去直段和实际弧长 | 通过 |
| 端口出入方向一致 | 通过 |
| 目标法线反向错误会被发现 | 通过 |
| 整体刚体旋转不改变内部线长 | 通过 |
| 低于线材最小半径的拐弯被拒绝 | 通过 |
| 两相邻弯曲挤占同一短段被拒绝 | 通过 |
| 端口直出空间不足被拒绝 | 通过 |

## 范围

没有EM/供电求解、流路压力/阻力求解、完整机柜耦合或区块存储实现；也没有验证真实物理精度、任务吞吐和任意设计可用。这里用于排除明显计量错误并帮助讨论玩法。
