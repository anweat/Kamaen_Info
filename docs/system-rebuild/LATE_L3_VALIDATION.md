# L3设计范围与有限验证

运行：`python -X utf8 docs/system-rebuild/build_late_l3.py`。生成器仅更新本目录中的L3输出，不覆盖R1历史或旧源文档。

结果：**32/32项通过**。

| 检查 | 结果 | 证据与范围 |
| --- | --- | --- |
| 旧能力全部保留且新增ID唯一 | 通过 | 保留78项，当前86项；包含新增谱纹/场域等能力 |
| 首次依赖全部存在 | 通过 | requires_all为AND；这里只描述能力，不把整段任务顺序当解锁条件 |
| 整合能力图可达且无环 | 通过 | 86/86项可从起点取得 |
| 移除全部可选深化仍能建设并返回新世界 | 通过 | 排除29项深化，主线仍可达；保留其用途而不要求全收集 |
| 特定量子/透镜/三模式及训练课题不再统一挡住终局 | 通过 | 材料、区域与远端参照仍需取得，研究手段可选择 |
| 移除精密材料或世界参照都会限制边界收束 | 通过 | 两侧共同参与，不由单线提供全部终局能力 |
| 首套低温和约束件独立于视界及其残渣 | 通过 | 普通制冷、材料、膜和独立停机可先取得 |
| 长期光存储和WDM不统一依赖视界存储 | 通过 | 普通持久介质与实际光网络先行；全息版本和RAM版本另列组件条件 |
| 所有物件ID唯一且有有效能力去向 | 通过 | 80条内容/实例/服务 |
| 实物投入都是已定义实体或前期工程份 | 通过 | 13种进口工程份，每种来源在内容表展开 |
| 不消耗知识记录合成实体 | 通过 | 资料和工位作为可复用条件，装配部件为实物包含关系 |
| 声明的实物投入图无自我材料循环 | 通过 | 此项不检查所有工装运行条件；关键首台的工装循环另在正文解释 |
| 旧阶段七至十一逐ID覆盖含旧别名 | 通过 | 68个ID含2个旧别名；无删除玩法处理 |
| R1已登记后期物件也全部有当前去向 | 通过 | 旧字典/额外表映射中的66项，逐项保留或明确服务化 |
| 同总剂量的顺序决定是否锁定 | 通过 | A4→B3=(1, 1)，B3→A4=(1, 0.0)；固定温窗教学投影 |
| 同工序划分成多步不额外赠送剂量 | 通过 | 只验证此指定无中途锁定反转的例子，非完整热/力学积分 |
| 并列出口分享同一输入预算 | 通过 | 输入20，两个实际出口各8，损耗4；两个对象不能各拿16 |
| 无人占用的出口仍保留传输份额 | 通过 | 另一个出口空置时，其8进入声明终端；不重新按当前工件枚举归一 |
| 零路径时能量去向仍完整 | 通过 | 无有效分配路径，输入全部进入声明源/终端损耗 |
| 相干分配共同计算两个出口 | 通过 | PA=13.506864, PB=2.493136, 传输合计16，损耗4 |
| C1冷端与热端预算一起成立 | 通过 | 冷量6至少输入45，热端排51；能力上限须另外满足 |
| 不同响应测头可区分本例两个来源 | 通过 | 观测5、7得到u=3、v=2；两个相同测头则不能单独辨识两源 |
| 同Z分布可有不同X联合统计 | 通过 | 有限态模板θ=π/4、φ=0/π；实际设备支持是额外条件 |
| 视界净输出与剩余材料不重复结算 | 通过 | 转化6份→500输出/100热，再支付维护20→净480/热120；4份残留 |
| 物理页与逻辑页分别计量 | 通过 | 双副本64物理页仅32逻辑页，索引另占空间 |
| 世界候选先满足约束再比较目标 | 通过 | 高分不能绕过通路与预算，未证明全部七组世界规则 |
| 接界的频差相位和实体方向改变实际传输 | 通过 | 指定四设置输出8/4/4/0，输入10；未传出的部分进入声明损耗，不另重扣同一因素 |
| 一次相同读数不能唯一反推边界参数 | 通过 | 频差与相位两候选最初均读4；追加相位对照后分别读2和0 |
| 平均负荷合格不能掩盖局部约束点超限 | 通过 | 平均7.5而第三点12超过上限10，样机拒绝按平均值继续接料 |
| 排热不足在同负载下累积而非重置热状态 | 通过 | 同初态与发热，排18降到38，排8升到48；仅核单周期预算，非完整停机仿真 |
| 新世界附加条件与工场共用实际维护供给 | 通过 | 保守静默工场需求9；增局部低载荷需求12，不能由10供给维持；完整适配另验 |
| 18份原始文档字节不变 | 通过 | 与SOURCE_MANIFEST中的长度和SHA-256比较 |

## 完成的设计层

- 后期首段、谱纹、空间工艺、世界切片、微观/量子与宇观、边界、视界三用途、世界之心、创世及持续使用均有玩法与用途。
- 每项旧后期ID有去向，实物给首次投入/工序，资料与服务给生成/运行条件；修正明显的材料与首台循环。
- 整合能力图保留前中期与全部旧能力，更新后期首次依赖，注明研究深入项。

## 仍未验证的实现层

- Minecraft GUI、方块/实体注册、原版行为适配、三维渲染与自动布线。
- 完整热/机械/光场、材料状态到所有响应的函数、全部运行工装条件与首个实际生产存档。
- 样机工程份拆回原版原料的全程数量、耗时、产能、回收与经济平衡。
- 实际量子装置/随机计数、全部维度规则组合、创建事务、多人/区块卸载与返回恢复。
- 玩家试玩与难度节奏；有限例子和图可达不等于游戏已经可通关。
