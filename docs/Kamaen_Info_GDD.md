# Kamaen Info - 模组核心设计文档 (GDD)

版本：v0.3 Design Draft  
状态：根据完整科技树、信息系统与硬件科技树文档整合后的核心 GDD。  

## 1. 核心定位

Kamaen Info 是一个围绕“物质、能量、信息和熵”展开的 Minecraft NeoForge 科技模组。玩家从圆石的宽频谱中提取基础资源，制造白噪晶种与卡玛恩晶体，再通过向量爆破、共振调谐、信息读取、掺杂晶体管、硬件自动化、世界底噪采样、凝聚态费曼过程、人造视界和黑洞信息设备，最终走向维度创世。

模组不追求在服务端 tick 内真实模拟神经网络、天体级黑洞或完整计算机。核心体验是把宏大的物理与信息概念落到有限、可诊断、可自动化的方块系统中。

主线科技树：

```text
圆石频谱
-> 白噪晶种
-> 卡玛恩晶体
-> 向量爆破与力学合成
-> 共振调谐与信息读取
-> 掺杂晶体管与电气合成
-> 卡玛恩硬件自动化
-> 世界底噪采样
-> 凝聚态费曼过程
-> 人造视界框架
-> 黑洞发电、存储、运算
-> 维度创世
```

## 2. 设计原则

- 前期永不硬卡死：圆石分馏与白噪晶种获取使用确定性进度，随机只影响副产物和品质。
- 宏大概念有限化：爆炸、黑洞、模型、量子协处理器和维度创世都以抽象数值、配方、预算和结构评分表现。
- 晶体数据统一：频谱签名、三轴张量、应力、掺杂浓度、噪声指数、相干度和底噪响应贯穿全线。
- 多方块结构缓存：机器只在结构变化时重扫，运行时读取缓存画像和少量动态状态。
- 信息读取有边界：探针只读取已注册 capability、adapter 或 `IKamaenReadable` 数据，不默认反射任意内部字段。
- 硬件支撑自动化：CPU、GPU/NPU、内存、存储和网络用于控制、诊断、调参和扩展玩法，主线不强制训练复杂模型。
- 模型系统可选拔高：训练、外部 API、自定义模型和量子协处理器能优化良率或发现隐藏属性，但主线材料有固定工艺窗口。

## 3. 核心资源与材料

### 3.1 圆石频谱

圆石被定义为包含大量基础物质可能性的宽频谱材料。玩家通过手摇频谱筛、搅拌分馏塔、滤膜和热噪坩埚，把圆石分解为基础粉末、频谱滤渣、均值化圆石和白噪晶种。

代表流程：

```text
圆石 + 水 -> 粗频谱泥浆
粗频谱泥浆 -> 基础粉末 + 频谱滤渣
频谱滤渣 + 热噪坩埚 -> 均值化圆石
均值化圆石 + 频谱滤渣 -> 白噪晶种
```

### 3.2 卡玛恩晶体

卡玛恩晶体是一种各向异性信息材料。它的价值不只是“高级矿物”，而是能记录并调制频谱、张量、应力、掺杂、相干性和底噪响应。

基础属性：

```text
spectrum_signature
axis_tensor
stress
noise_index
coherence
doping_concentration
background_noise_response
```

晶体在不同阶段承担不同角色：

- 力学时代：被向量爆破塑造成导波、抗压、稳谱或偏振晶体。
- 信息时代：作为共振催化器和 InfoSignal 封装介质。
- 电气时代：被掺杂、刻蚀和互连为 PN 结、二极管、晶体管和硬件模块。
- 凝聚态时代：承载准粒子、拓扑、超导、隧穿和费曼过程。
- 视界时代：参与人造视界约束、全息信息膜和维度级规则编译。

## 4. 合成框架系统

合成框架是贯穿全科技树的研究站和多步骤合成控制平台。它由成型方块、功能方块、传感器、结构材料、作用器、活动机构和接口组成。结构成型后生成 `FrameworkProfile`，用于判断当前工艺精度、可观测参数和可执行流程。

三代框架：

| 等级 | 阶段 | 主要能力 |
| --- | --- | --- |
| I 型力学合成框架 | 爆破、偏振、共振 | 向量、压力、冲击波、应力椭球、晶格对齐 |
| II 型电气合成框架 | 掺杂、光刻、晶体管 | 频谱、电荷、掺杂、互连精度、导通方向 |
| III 型视界合成框架 | 凝聚态、底噪、黑洞 | 世界底噪、相干性、准粒子、拓扑、熵流、视界边界 |

摆放位置提供真实参数来源，例如中心距离、轴向排列、对称结构、闭环约束、线路长度差、传感器视线和活动自由度。

## 5. 阶段玩法

### 5.1 圆石初炼

目标是从圆石中稳定提取基础资源，并获得白噪晶种。初级分光镜显示频谱、峰值偏差和白噪潜势。连续处理相同输入会积累谱线污染，需要更换滤膜、调整石材或升级搅拌轴。

### 5.2 向量爆破与力学合成

爆炸室不调用大范围真实爆炸，而是计算 `VectorImpulse`：

```text
direction + strength + waveform_purity + impulse_width
```

理想刚体板、缓冲层、靶座和传感器决定方向匹配、能量窗口、波形纯净度和应力稳定度。玩家通过爆破评分制造粗卡玛恩晶体、导波晶体、抗压晶体、稳谱晶体和偏振晶体。

### 5.3 共振调谐与信息读取

晶体干涉仪读取晶体张量和应力，频谱锁相器把目标物品或机器的固有频谱写入晶体。卡玛恩探针台把可读数据转为统一的 `InfoSignal`：

```text
type
value
frequency_vector
confidence
timestamp
```

`InfoSignal` 可以进入频率总线、红石转换模块、调谐阵列和自动化控制系统。

### 5.4 掺杂晶体管与电气合成

电气时代引入离子注入、退火、光刻、刻蚀和封装。P 型晶片偏信息门控，N 型晶片偏能量传输，PN 结用于二极管、阈值门、过滤器和晶体管阵列。

代表流程：

```text
稳谱卡玛恩晶体 -> 切片 -> 晶片坯
晶片坯 + 施主掺杂剂 -> N 型卡玛恩晶片
晶片坯 + 受主掺杂剂 -> P 型卡玛恩晶片
N 型晶片 + P 型晶片 + 退火 -> 卡玛恩 PN 结
PN 结 + 光刻掩膜 + 刻蚀 -> 卡玛恩晶体管
晶体管 + 互连线 + 晶振 -> 硬件模块
```

高精度工艺使用相干能。相干能不是更大的 FE，而是由稳谱晶体把普通 FE 整理出的低噪、可锁相工艺能源。

### 5.5 硬件自动化

硬件系统由 CPU、GPU/NPU、主板、内存页、参数页、储存、频率晶振、光纤和无线网络组成。它用于自动化生产、传感器协调、路由、模型部署、工艺诊断和参数调节。

频率等级：

```text
F1 控制频段：红石转换、触发器、低速 InfoSignal、小型控制器。
F2 数据频段：模型内网、内存页、参数页、小模型部署、主板通信。
F3 矩阵/激活频段：GPU/NPU 激活流、batch 数据、工艺预测。
F4 光频协议：训练集群、多卡互连、高速模型服务。
F5 维度锁相协议：跨维度通信、世界之心、创世编译。
```

诊断台输出多维性能画像，而不是单一等级，包括 `control_ops`、`route_ops`、`matrix_ops`、`operator_match`、`memory_pages`、`frequency_tier` 和带宽指标。

### 5.6 世界底噪与宏观信息中心

玩家从读取方块状态推进到读取维度运行时的背景信息场。世界底噪不是单一资源，而是一组可扩展层：

```text
ThermalNoiseLayer
RadiationNoiseLayer
QuantumNoiseLayer
BlockUpdateNoiseLayer
EntityBehaviorLayer
RedstoneNoiseLayer
BiomeNoiseLayer
VoidNoiseLayer
StringResidueLayer
```

信息中心组织探针、传感器、硬件节点、底噪数据库和频率总线，形成长期采样、归档、路由和控制系统。

### 5.7 凝聚态费曼过程

后期材料线使用凝聚态物理概念。费曼图作为准粒子过程的合成语法，而不是高能物理装饰：

```text
外线 = 输入/输出准粒子或场
顶点 = 允许的相互作用
传播子 = 中间态
闭环 = 修正项、噪声、自能
守恒约束 = 电荷、动量、频率、拓扑数、晶格对称性
```

玩家通过底噪样本、准粒子捕获器、费曼过程编译台、低温凝聚腔、拓扑相变炉和隧穿耦合器制造超导卡玛恩环、拓扑绝缘晶体、相变记忆晶体、准粒子滤波器和 4 维电路桥。

### 5.8 量子协处理器

量子协处理器不是万能计算机，只负责微观模拟、路径采样、波函数稳定窗口和凝聚态预测。它接收费曼过程蓝图和材料状态，输出合成参数曲线，由 III 型框架执行。

### 5.9 人造视界与黑洞科技

戈古尔斯弦是维度初始边界条件的残留映射。它与世界底噪数据库、凝聚态约束材料和 III 型视界框架合并后，可形成有限多方块内的可控人造视界。

黑洞科技分为三类用途：

- 发电：吸积盘能量抽取、彭罗斯过程模拟、霍金辐射回收、熵流差发电。
- 存储：事件视界信息膜、全息编码、霍金辐射校验、底噪冗余写入。
- 运算：熵梯度搜索、微观态枚举、路径坍缩求解、费曼过程筛选、维度参数冲突检测。

服务端只计算质量评分、稳定度、相干度、熵流、冷却、控制精度、封顶 FE 输出、信息页和运算预算；视觉效果放在客户端表现。

### 5.10 维度创世

最终阶段提取当前维度的信息特征，结合戈古尔斯弦、世界底噪数据库、黑洞运算结果和参数化模板生成新维度。

核心流程：

```text
戈古尔斯弦 + 世界之心频率 + 维度规则候选 -> 创世蓝图
创世蓝图 + 维度参数矩阵 + 视界能量缓存 -> 已编译维度核心
已编译维度核心 + 维度点火环 -> 新维度入口
空间锚 + 已编译维度核心 -> 固定入口锚点
```

维度参数包括地形基底、群系权重、物理规则、资源频谱、实体生成、底噪结构和熵增速率。第一版优先使用预注册模板和固定槽位，避免运行时动态注册灾难。

## 6. 信息系统与模型扩展

信息系统的基础闭环：

```text
世界状态
-> 传感器采样
-> 类型化数据
-> 脚本器预处理/融合
-> 模型外网输入
-> 模型内网调度
-> 参数空间/算子网络推理
-> 输出路由
-> 执行器影响世界
-> 反馈信号回流
```

数据分为 Input、Parameter、State、Activation。loss、reward、metric 和 feedback 不作为特殊第五类数据，而是普通数据节点在训练、评价或调度语境下的角色。

模型系统支持参数晶胞、参数矩阵板、参数晶体簇、参数库、算子、脚本器、触发器、模型内网、模型外网、反馈节点、实验日志和外部 API。它是可选上限玩法，用于优化复杂工艺、自动化实体行为、物流控制、诊断和数据分析。

## 7. 实现边界

- 爆炸室只做向量矩阵和结构评分，不做真实爆炸射线。
- 探针读取统一走 capability、adapter 和白名单接口。
- 硬件与模型只结算抽象性能画像，不强制运行真实模型。
- 外部 API 可选，必须有延迟、成本、失败、权限、限流、超时和离线 fallback。
- 量子协处理器只输出参数窗口与评分，不模拟真实量子态。
- 黑洞装置不吸引实体，不模拟真实天体黑洞。
- 维度创世优先使用预注册模板、数据包配置和参数矩阵。

## 8. MVP 与版本路线

第一阶段目标是建立可玩的“圆石分馏 -> 白噪晶种 -> 粗卡玛恩晶体 -> 信息读取”闭环。

| 版本 | 目标 |
| --- | --- |
| v0.1 | 分馏塔、基础资源、频谱滤渣、白噪晶种 |
| v0.2 | 模拟爆炸室、理想刚体、粗卡玛恩晶体、晶体 Data Component |
| v0.3 | 晶体干涉仪、探针基础读取、InfoSignal、频率总线 |
| v0.4 | I 型力学合成框架、偏振晶体、共振催化晶体 |
| v0.5 | 离子注入器、P/N 型晶片、晶体二极管和过滤器 |
| v0.6 | II 型电气合成框架、光刻、刻蚀、晶体管和基础硬件自动化 |
| v0.7 | 硬件诊断台、F1-F3 晶振、基础 CPU/GPU/NPU 模块、自动化生产闭环 |
| v0.8 | 世界底噪采样、底噪数据库、宏观信息中心 |
| v0.9 | 凝聚态材料、费曼过程蓝图、量子协处理器、III 型视界合成框架 |
| v1.0 | 人造视界、黑洞发电/存储/运算、维度创世和维护系统 |

## 9. 参考设计文档

- `docs/Kamaen_Info_TechTree_Plan.md`：完整科技树与实现方案。
- `docs/Kamaen_Info_Information_System_Design.md`：信息系统、模型运行、传感器、路由、反馈和外部 API。
- `docs/Kamaen_Info_Hardware_TechTree_Design.md`：CPU/GPU/NPU、晶振协议、主板、内存、通信网络和诊断系统。
